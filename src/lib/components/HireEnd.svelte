<style>
	.hire-end {
		position: relative;
		z-index: 2;
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
		background: var(--color-background);
	}

	.hire-text {
		font-size: clamp(3rem, 9vw, 7.5rem);
		font-weight: 700;
		letter-spacing: -0.02em;
		line-height: 0.95;
		text-transform: uppercase;
		color: var(--color-heading);
		text-align: center;
		padding: 0 1rem;
		margin: 0;
	}

	.hire-text em {
		font-style: normal;
		color: var(--color-primary);
	}
</style>

<section class="hire-end">
	<p class="hire-text">Definitely<br /><em>hire me!</em></p>
</section>
